import { describe, expect, it } from "vitest";
import { forwardDeviceHubEvent } from "../src/devicehub/client.js";
describe("Device Hub cross-repo contract SHA 16a3bc665ebc6b6f0784b6e853004a4d6667ea79",()=>{it("sends strict payload and never sends school authority",async()=>{let seen:any;const r=await forwardDeviceHubEvent("https://school.example",{instance_id:"11111111-1111-4111-8111-111111111111",device_id:"22222222-2222-4222-8222-222222222222",raw_provider_event_id:"evt-1",credential_type:"card",event_type:"check_in",occurred_at:new Date().toISOString(),metadata:{school_id:"forged"}},"s".repeat(32),async(u,o)=>{seen={u,o};const b=JSON.parse(String(o?.body));const allowed=["device_id","raw_provider_event_id","external_person_id","credential_type","event_type","occurred_at","metadata"];if(Object.keys(b).some(k=>!allowed.includes(k))||"school_id" in b||"instance_id" in b||"type" in b||"payload" in b)return new Response("bad",{status:400});return new Response("ok",{status:200})});expect(r.status).toBe(200);expect(seen.o.headers["x-schoolsafe-instance"]).toBe("11111111-1111-4111-8111-111111111111");expect(JSON.parse(seen.o.body)).not.toHaveProperty("school_id");});});
