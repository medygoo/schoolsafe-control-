import { signRequest } from "../auth/hmac.js";
export type DeviceHubEvent={event_id:string;device_id:string;instance_id:string;school_id:string;type:string;occurred_at:string;payload:Record<string,unknown>};
export async function forwardDeviceHubEvent(target:string,event:DeviceHubEvent,secret:string,fetchImpl:typeof fetch=fetch):Promise<Response>{ const url=new URL("/machine/devicehub/events",target); if(url.protocol!=="https:") throw new Error("HTTPS_REQUIRED"); const body=JSON.stringify(event); const timestamp=Math.floor(Date.now()/1000); const signature=signRequest({method:"POST",path:url.pathname+url.search,body,timestamp,secret}); return fetchImpl(url,{method:"POST",headers:{"content-type":"application/json","x-schoolsafe-instance":event.instance_id,"x-schoolsafe-timestamp":String(timestamp),"x-schoolsafe-signature":signature},body}); }
